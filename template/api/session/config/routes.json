{
  "routes": [
    {
      "method": "GET",
      "path": "/sessions",
      "handler": "session.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sessions/:id",
      "handler": "session.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
