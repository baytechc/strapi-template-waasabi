{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "event-manager.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/webhooks",
      "handler": "webhooks.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/signals",
      "handler": "signal.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/signals",
      "handler": "signal.create",
      "config": {
        "policies": []
      }
    }
  ]
}
