{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "event-manager.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/webhooks",
      "handler": "webhooks.index",
      "config": {
        "policies": []
      }
    }
  ]
}
